name: "My workflow"
on:
  workflow_dispatch:

jobs:
  job1:
   runs-on: ubuntu-latest
   steps:
      - name: Run the scripts
        run: pwd 
        env:
         MY_ENV_VAR: ${{ github.ref == 'refs/heads/main' && false || 'value_for_other_branches' }}
        


