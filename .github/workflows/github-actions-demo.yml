name: "My workflow"
on:
  workflow_dispatch:
jobs:
  job1:
    runs-on: ubuntu-latest
    concurrency:
      group: group1
      cancel-in-progress: false
    steps:
      - id: step1
        run: sleep 120
