on:
  workflow_dispatch:
jobs:
  job1:
   runs-on: ubuntu-latest
   steps:
    - name: Set the value
      id: step_one
      run: |
       {
        echo 'JSON_RESPONSE<<EOF'
        curl https://example.com
        echo EOF
        } >> "$GITHUB_ENV"
    - name: Use the value
      id: step_two
      run: |
       printf '%s\n' "$JSON_RESPONSE"
   
